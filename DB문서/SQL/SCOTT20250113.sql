select *
from emp;

--CRUD //기본이자 최고 실력자

CREATE TABLE TEST(
	STR VARCHAR(1000),
	STR_LIST VARCHAR2(1000),
	NUM NUMBER(8, 2),
    NUM_MAX NUMBER,
	HIREDATE DATE
);

DESC TEST;

SELECT *
FROM TEST;

CREATE TABLE EMP01(
    EMPNO NUMBER(4),
    ENAME VARCHAR2(20),
    SAL NUMBER(7,2)
);

DESC EMP01;

SELECT * FROM USER_SYS_PRIVS;

--테이블 삭제
DROP TABLE EMP01;

CREATE TABLE EMP03
AS -- 별칭
SELECT EMPNO, ENAME 
FROM EMP;

DESC EMP03;

SELECT *
FROM EMP03;

CREATE TABLE EMP04
AS
SELECT EMPNO, ENAME 
FROM EMP
WHERE 1 = 0;

DESC EMP04;

SELECT *
FROM EMP04;

-- 공집합 결과 
SELECT EMPNO, ENAME 
FROM EMP
WHERE 1 = 0;

--테이블 구조를 변경하는 ALTER
DESC EMP01;

--표현식
--ALTER TABLE 테이블명
--ADD 추가할 컬럼명 데이터 유형;

ALTER TABLE EMP01
ADD(JOB VARCHAR2(9));

DESC EMP01;

DESC EMP04;

--표현식
--ALTER TABLE 테이블명
--DROP COLUMN 삭제할 컬럼명;

ALTER TABLE EMP01
DROP COLUMN SAL;

DESC EMP01;

ALTER TABLE EMP01
ADD(JOB VARCHAR2(9));

ALTER TABLE EMP01
ADD(SAL NUMBER(8, 2));

ALTER TABLE EMP01
MODIFY SAL NUMBER(7, 2);

DESC EMP01;

ALTER TABLE EMP01
MODIFY (SAL VARCHAR2(10), JOB VARCHAR2(90));

DROP TABLE EMP04;

SELECT *
FROM EMP04;

CREATE TABLE EMP04
AS
SELECT *
FROM EMP;

TRUNCATE TABLE EMP04;

SELECT *
FROM EMP04;

DROP TABLE TEST;

DESC EMP;

CREATE TABLE TEST
AS
SELECT EMPNO, ENAME, SAL, HIREDATE DD
FROM EMP
WHERE 1 = 0;

DESC TEST;

SELECT *
FROM TEST;

INSERT INTO TEST
VALUES (1, '하하', 100.02, NULL);

INSERT INTO TEST
VALUES (1, '하하', 100.02, NULL);

INSERT INTO TEST 
(EMPNO, ENAME, SAL, DD)
VALUES (2, '하하2', 100.03, NULL);

INSERT INTO TEST 
(ENAME, SAL, EMPNO)
VALUES ('신입1', 70, -9999);

SELECT *
FROM TEST;

INSERT INTO TEST 
VALUE(ENAME, SAL, EMPNO, DD)
VALUES('신입3', 700, -1, SYSDATE);

SELECT *
FROM TEST;

DESC TEST;

INSERT INTO TEST 
VALUE(ENAME, SAL, EMPNO, DD)
VALUES('숫자', 12345.12, -1, SYSDATE);

SELECT *
FROM TEST;

INSERT INTO TEST 
VALUE(ENAME, SAL, EMPNO, DD)
VALUES('숫자', 12345.12, 12345, '50/12/17');

DESC TEST;

SELECT TO_CHAR(DD, 'YYYY')
FROM TEST;


--서로 다른 테이블에 데이터 입력하기
--서로 다른 테이블에 다중행 데이터 입력하기

SELECT *
FROM TAB;


SELECT *
FROM TEST;

TRUNCATE TABLE TEST;

INSERT INTO TEST
SELECT EMPNO, ENAME, SAL, HIREDATE
FROM EMP;

SELECT MAX(LENGTH(ENAME))
FROM TEST;

DESC TEST;

--MODIFY 주의사항
ALTER TABLE TEST
MODIFY ENAME VARCHAR2(15);

ALTER TABLE TEST
MODIFY SAL VARCHAR2(100);

SELECT *
FROM TEST;

SET DEFINE OFF;

UPDATE TEST
SET SAL = 100;

UPDATE TEST
SET ENAME = 'SDFSDF';

UPDATE TEST
SET ENAME = 'SMITH&미세스', SAL = SAL * 100
WHERE ENAME = 'SMITH';

SELECT *
FROM TEST;


DELETE FROM TEST
WHERE ENAME = 'FORD';





