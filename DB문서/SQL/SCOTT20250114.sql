DROP TABLE DEPT1;

CREATE TABLE DEPT1
AS
SELECT *
FROM DEPT;

SELECT *
FROM DEPT1;

DELETE FROM DEPT1;

ROLLBACK;

DESC DEPT1;

INSERT INTO DEPT1
VALUE(DEPTNO, DNAME, LOC)
VALUES(1, 'A', 'AA');

SELECT *
FROM DEPT1;

INSERT INTO DEPT1
VALUE(DEPTNO, DNAME, LOC)
VALUES(2, 'B', 'BB');

INSERT INTO DEPT1
VALUE(DEPTNO, DNAME, LOC)
VALUES(3, 'C', 'CC');

ROLLBACK;
COMMIT;


------------

select *
FROM EMP;

SELECT *
FROM EMP
WHERE ENAME = 'SMITH';

SELECT *
FROM DEPT
WHERE DEPTNO = 20;


SELECT *
FROM EMP;

SELECT DEPTNO
FROM EMP
WHERE ENAME = 'SMITH';

SELECT *
FROM DEPT;

SELECT DNAME
FROM DEPT
WHERE DEPTNO = 20;


--기술 대체

--SMITH의 부서명이 무엇인지 알아내는 SQL문을 작성하시오!

SELECT *
FROM EMP, DEPT
WHERE EMP.DEPTNO = DEPT.DEPTNO
and ename = 'SMITH';

DESC EMP;
DESC DEPT;


--서로 다른 테이블을 연결해서 질의 하는 방법
SELECT *
FROM EMP, DEPT
WHERE EMP.DEPTNO = DEPT.DEPTNO
and EMP.ename = 'SMITH';


